#english commands'
#DO NOT change the keys' prefixes (first string before the dot), as they are used in the code to identify the command type

deleteindex.command=delete

indexsite.command=index^site

indexsitemap.command=index^sitemap

indexpage.command=index^page

hello.command=hello
